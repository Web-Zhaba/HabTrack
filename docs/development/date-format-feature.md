# Настройка формата даты

Дата: 24.02.2026
Статус: ✅ Реализовано (с сохранением в localStorage)

---

## 📋 Описание

Добавление возможности пользователю настраивать формат отображения дат в приложении.

### Реализовано
- ✅ **Пресеты** (кнопки) для быстрого выбора популярных форматов
- ✅ **Кастомный инпут** с использованием `AnimatedInput` для консистентности дизайна
- ✅ **Превью** как будет выглядеть дата
- ✅ **Валидация** введённого формата (проверка на наличие дня и года)
- ✅ **Хранение в Redux** settingsSlice
- ✅ **Сохранение в localStorage** — формат и имя пользователя сохраняются между сессиями
- ✅ **Хук useDateFormat** для централизованного форматирования
- ✅ **Интеграция в Settings page**
- ✅ **Удалена заглушка** из SettingsLocaleCard
- ✅ **Обновлены места использования format()**:
  - `HomeHeader.tsx`
  - `HomeMiniStats.tsx`
  - `HomeHabitsList.tsx`

---

## 🎨 UI Дизайн (Вариант A: Вкладки)

```
┌─────────────────────────────────────┐
│ Формат даты                         │
├─────────────────────────────────────┤
│ [ Пресеты | Кастомный ]             │
├─────────────────────────────────────┤
│ ○ 24.02.2026 (DD.MM.YYYY)          │
│ ○ 02/24/2026 (MM/DD/YYYY)          │
│ ○ 2026-02-24 (YYYY-MM-DD)          │
│ ○ Свой формат:                      │
│   [MM-DD-YYYY____________]          │
├─────────────────────────────────────┤
│ Пример: 02-24-2026                  │
└─────────────────────────────────────┘
```

---

## 📁 Структура файлов

```
src/features/settings/
├── components/
│   └── DateFormatPicker.tsx    # Компонент выбора формата
├── hooks/
│   └── useDateFormat.ts        # Хук для форматирования дат
└── store/
    └── settingsSlice.ts        # Добавить dateFormat
```

---

## 🔧 Технические детали

### Допустимые токены date-fns

| Токен  | Пример    | Описание           |
|--------|-----------|-------------------|
| `dd`   | 01-31     | День месяца       |
| `MM`   | 01-12     | Месяц             |
| `yyyy` | 2026      | Год (4 цифры)     |
| `yy`   | 26        | Год (2 цифры)     |
| `EEEE` | вторник   | День недели       |
| `eee`  | вт        | День недели (кр.) |

### Хранение в Redux

```typescript
interface SettingsState {
  userName: string;
  dateFormat: string; // 'dd.MM.yyyy' по умолчанию
}
```

### Хук useDateFormat

```typescript
export function useDateFormat() {
  const format = useAppSelector(selectDateFormat);
  
  const formatDate = (date: Date | string) => {
    return format(new Date(date), format, { locale: ru });
  };
  
  return { formatDate, format };
}
```

---

## ✅ План реализации

- [ ] 1. Обновить `settingsSlice` — добавить `dateFormat` и экшены
- [ ] 2. Создать хук `useDateFormat` для централизованного форматирования
- [ ] 3. Создать компонент `DateFormatPicker` с превью
- [ ] 4. Интегрировать в Settings page
- [ ] 5. Обновить все места где используется `format()` на хук
- [ ] 6. Добавить валидацию формата
- [ ] 7. Сохранение в localStorage

---

## 📍 Места для обновления

Где используется форматирование дат:

| Файл | Текущее использование |
|------|----------------------|
| `HomeHabitsList.tsx` | `formatDateKey()` |
| `HomeHeader.tsx` | `format(today, "d MMMM yyyy,EEEE", { locale: ru })` |
| `HomeMiniStats.tsx` | `formatDateRange()` |
| `UnifiedCalendar.tsx` | Разные форматы |
| `WeeklyCalendar.tsx` | Заголовки дней |

---

## 🔗 Связанные документы

- [[Refactoring Suggestions](./refactoring-suggestions.md)]
- [[Settings Store](../features/settings/store/settingsSlice.ts)]
