📜 Project Vision: NODES (Habit Tracker Network)
1. Философия и Метафора
Проект уходит от классического понимания «списка дел» в сторону визуализации жизненных паттернов. Вместо сухих галочек пользователь строит интерактивную нейронную сеть своей личности.
 * Node (Узел): Атом деятельности (ранее — привычка).
 * Connector (Коннектор): Семантическая связь (тег), объединяющая узлы в кластеры.
 * Pulse (Импульс): Акт выполнения действия, питающий узел энергией.
 * Stability (Стабильность): Показатель жизнеспособности узла (ранее — стрик).
 * Network (Сеть): Итоговая карта прогресса и социальное окружение.
2. Архитектурный Стек (Tech Stack)
Проект строится на принципах масштабируемости и чистоты кода для новичка:
 * Frontend: React + TypeScript.
 * State Management: Redux Toolkit (нормализованная структура данных для быстрого доступа по ID).
 * Styling: Tailwind CSS + Shadcn UI (компоненты на базе Radix UI).
 * Architecture: Feature-based Design (группировка кода по функциональным фичам, а не типам файлов).
 * Visualization: react-force-graph (движок для симуляции физических связей Obsidian-style).
 * Data Persistence: Redux Persist (хранение данных в localStorage на первых этапах).
3. Информационная Модель (Data Schema)
Каждый узел описывается плоской структурой для удобства фильтрации и маппинга в граф:
interface AppNode {
  id: string;           // UUID
  label: string;        // Название (напр. "Coding")
  connectors: string[]; // Массив тегов (напр. ["career", "focus"])
  pulseCount: number;   // Количество выполнений
  isCompletedToday: boolean;
  lastPulse: string;    // ISO дата последнего выполнения
  authorId: string;     // Для социального взаимодействия
  isPublic: boolean;    // Видимость в глобальной сети
}
В UX-дизайне есть риск: если метафора слишком сложная, пользователь запутается и уйдет. Твоя задача — провести его за руку от знакомого понятия к твоему новому «крутому» названию.
Вот 3 стратегии, как это сделать мягко и понятно:
4. Онбординг: Метафора «Связей»
При первом входе на сайт (или в пустом состоянии списка) покажи короткий туториал из 2-3 слайдов.
 * Слайд 1: «Привычка — это не просто действие в списке. Это Node (Узел) вашей жизни».
 * Слайд 2: «Каждый раз, когда вы выполняете действие, вы посылаете Pulse (Импульс), укрепляя этот узел».
 * Слайд 3: «Общие теги создают Связи. Так ваши ритуалы превращаются в единую живую систему».
2. Подсказки в интерфейсе (Tooltips)
Используй микро-копирайтинг, который переводит с «программистского» на «человеческий».
 * Заголовок формы: вместо просто «Add Node», напиши: Initialize New Node (Habit). Со временем слово в скобках можно будет убрать.
 * Placeholder в инпуте: What habit will become your next node? (Какая привычка станет вашим следующим узлом?).
 * Информационная иконка (i): Рядом с заголовком графа добавь иконку, при наведении на которую всплывает текст:
   > "Nodes — это ваши ежедневные действия. Теги связывают их в нейронную сеть вашего прогресса".
   > 
3. Геймификация через «Энергию»
Людям понятнее, когда «Node» ведет себя как живой объект.
 * Если пользователь давно не отмечал привычку, узел в списке и на графе становится тусклым и «теряет связь».
 * В статус-баре карточки напиши: Node Stability: 85% (вместо «Стрик: 10 дней»). Это интригует и заставляет пользователя изучать, что это значит.
Текст для страницы «О проекте» или FAQ
Если пользователь захочет почитать подробнее, объясни это так:
> Почему Nodes, а не списки?
> Обычные трекеры — это просто чек-листы. Но наша жизнь сложнее. Чтение книги связано с обучением, а бег — со здоровьем и настроением. Мы называем привычки Узлами, потому что они — точки пересечения ваших интересов. С помощью тегов-коннекторов вы увидите, как ваши действия влияют друг на друга, создавая уникальную карту вашей личности.
> 
Практический совет по коду:
В компоненте CreateNodeForm можно добавить небольшой текст-подсказку прямо под инпутом:
<p className="text-[10px] text-slate-500 mt-2">
  <span className="text-cyan-500 font-bold">Concept:</span> Nodes are your daily rituals. 
  Tags act as neural links between them.
</p>

4. Визуальный Гайд (UI Style Guide)
Стиль проекта: Cyber-Minimalism.
 * Эффекты: * Glow Effect: Активные карточки и узлы графа подсвечиваются внешним свечением box-shadow: 0 0 15px cyan.
   * Pulse Animation: При отметке узла кнопка и узел в графе визуально "детонируют" волной.
5. UX-стратегия и Онбординг
Чтобы не пугать пользователя сложной терминологией:
 * Постепенное раскрытие: На старте интерфейс выглядит как чистый список. Граф открывается как «Продвинутый вид».
 * Двойной нейминг: В подсказках всегда указывается Node (Habit) и Connector (Tag).
 * Интерактивный граф: Граф — это не просто картинка, а пульт управления. Клик по узлу открывает его настройки.
6. Роадмап разработки (Roadmap)
Этап 1: Основание (MVP 1.0)
 * [ ] Определение стэка и нейминга.
 * [ ] Реализация Redux-хранилища с нормализацией.
 * [ ] Создание компонента NodeCard и формы CreateNode.
 * [ ] Базовый список NodeList с фильтрацией по коннекторам.
Этап 2: Визуализация (V 2.0)
 * [ ] Подключение react-force-graph.
 * [ ] Реализация селекторов-трансформеров (Data -> Graph Format).
 * [ ] Настройка физики графа (притяжение по общим тегам).
Этап 3: Социальный слой (V 3.0)
 * [ ] Система профилей и авторизация.
 * [ ] Глобальная лента «пульсов» (кто что сделал сейчас).
 * [ ] Функция «Слияние сетей» — просмотр графов друзей и копирование их узлов.
Этап 4: Полировка (V 4.0)
 * [ ] Сложная аналитика (графики стабильности).
 * [ ] Темные/Светлые неоновые темы.
 * [ ] Мобильное приложение (PWA).
